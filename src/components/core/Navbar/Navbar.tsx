import { Box } from '@mui/material';
import React from 'react';

export const Navbar = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'space-between',
        width: 1,
        height: '14vh',
      }}
    ></Box>
  );
};
